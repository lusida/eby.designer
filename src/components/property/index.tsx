import React from "react";
import "./index.less";

interface Props {
    schema: ISchema;
}

const PropertyBar: React.FC<Props> = (props) => {

    return (
        <div className="eby-property">
            
        </div>
    );
}

export default PropertyBar;